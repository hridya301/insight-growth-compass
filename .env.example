
# In a browser environment, the MongoDB connection isn't used directly.
# This would be used in a server-side implementation.
# The frontend mock database will work without this.
VITE_MONGODB_URI=mongodb+srv://username:password@clustername.mongodb.net/databasename?retryWrites=true&w=majority
